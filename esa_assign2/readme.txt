cu->cart and user
p->product

first install dependencies npm install.
It currently listens to port 3000 , can change manually if that port is busy within the code

then at the directory where source is located type in npm run dev and that'll set the program running

Postman can be used to send requests to the backend and see what's going on.
The files in the routes folders can be used to see , what happens with reqs and res and the contents of the body of the req wherever appropriate
